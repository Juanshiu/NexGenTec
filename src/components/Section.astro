---
const { class: className, id } = Astro.props
---

<section id={id} class={`${className} flex max-w-6xl h-auto mx-auto p-5 gap-4 bg-white mt-8 rounded-lg border shadow-lg`}>
    <slot />
</section>